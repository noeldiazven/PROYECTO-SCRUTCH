#ifndef TIPOS
#define TIPOS
#include<QString>

typedef QString(IDS);
typedef double(entero);
typedef int(tipo_entero);
typedef double(dobles);
typedef bool(buleo);
typedef float(flotando);



#endif // TIPOS

